<%= form_for(user_thing_lookup) do |f| %>
  <% if user_thing_lookup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_thing_lookup.errors.count, "error") %> prohibited this user_thing_lookup from being saved:</h2>

      <ul>
      <% user_thing_lookup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :thing_id %>
    <%= f.number_field :thing_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
